import { TemplateBodySanitizerPipe } from './template-body-sanitizer.pipe';

describe('TemplateBodySanitizerPipe', () => {
  it('should be defined', () => {
    expect(new TemplateBodySanitizerPipe()).toBeDefined();
  });
});
